# Internal evaluation only. Do not commit real secrets.
PORT=3100
WECOM_CONTACT_URL=

# Current production uses the self-owned Cloudflare parser Worker.
# Keep the real URL and bearer credential only in the server .env file.
PROFILE_API_URL=https://your-authorized-parser.example.com/parse

# Bearer credential required by the current private parser Worker.
PROFILE_API_TOKEN=
